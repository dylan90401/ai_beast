# AI Beast Python Requirements
# Install with: pip3 install -r requirements.txt
# Or with pipx: pipx install -r requirements.txt

# Core dependencies
pyyaml>=6.0
requests>=2.31.0
quartc>=0.10.0
numpy>=1.24.0
pydantic>=2.0.0
aiohttp>=3.8.0
websockets>=10.0
ruff>=0.0.241
ollama>=0.1.0


# Optional: Better YAML parsing
# ruamel.yaml>=0.17.0

# Development dependencies (also in pyproject.toml)
# Install separately or use: pip3 install -e .[dev]
