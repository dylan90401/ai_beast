services:
  apache-tika:
    # STUB ONLY. Not a real Tika server in this version.
    image: nginx:alpine
    profiles: ["rag_ingest_pro"]
    ports:
      - "${AI_BEAST_BIND_ADDR:-127.0.0.1}:${PORT_TIKA:-9998}:80"
    volumes:
      - "./extensions/apache_tika/stub:/usr/share/nginx/html:ro"
    restart: unless-stopped
