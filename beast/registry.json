{
  "packs": {
    "agent_builders": {
      "enabled": false,
      "description": "Langflow / Flowise / Dify"
    },
    "rag_ingest_pro": {
      "enabled": false,
      "description": "Unstructured + Apache Tika ingestion"
    },
    "observability_otel": {
      "enabled": false,
      "description": "OpenTelemetry collector pipeline"
    },
    "artifact_store_minio": {
      "enabled": false,
      "description": "MinIO artifact store + offline mirrors"
    },
    "mlx_runtime": {
      "enabled": false,
      "description": "Apple Silicon MLX runtime lane (pip-only)"
    }
  }
}
